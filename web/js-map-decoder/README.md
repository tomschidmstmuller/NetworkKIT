# Javascript Map Decoder

In order to use this script, put all the map files in the `map` folder. The scripts respects the application's folder structure and will create all necessary folders. Decoded files will be placed in the `source-code` folder.

To run the script run:

```bash
git clone <repo>
npm install
node decode-source-map.js
```